training loss measures fit , validation loss measures generalization .
cross validation is a procedure that rotates the validation fold so every row gets scored exactly once . it is an evaluation practice idea . use cross validation when the data set is small and a single split would be noisy . a data scientist reaches for cross validation as part of a evaluation practice workflow .
perplexity is the exponential of the average cross entropy , so lower is better . it is an evaluation metric idea . use perplexity when you are scoring a language model on held out text . a data scientist reaches for perplexity as part of a evaluation metric workflow .
a model is only as honest as the split it was scored on .
mean squared error is the average of the squared gaps between predictions and targets . it is an evaluation metric idea . use mean squared error when you are scoring a regression model and large errors should hurt more . a data scientist reaches for mean squared error as part of a evaluation metric workflow .
swiglu is a feed forward block that gates one linear projection with a swish activation of another . it is a transformer component idea . use swiglu when you want a stronger feed forward block at the same parameter budget . a data scientist reaches for swiglu as part of a transformer component workflow .
gradient boosting is an ensemble that adds trees one at a time so each tree fits the errors of the ones before it . it is a supervised learning idea . use gradient boosting when you want the best accuracy you can get on tabular data . a data scientist reaches for gradient boosting as part of a supervised learning workflow .
training loss measures fit , validation loss measures generalization .
linear regression is a model that fits a straight line through the data by minimizing squared error . it is a supervised learning idea . use linear regression when you need a fast interpretable baseline for a numeric target . a data scientist reaches for linear regression as part of a supervised learning workflow .
lower cross entropy on held out text means the model is less surprised by it .
lower cross entropy on held out text means the model is less surprised by it .
dropout is randomly zeroing activations during training so the network cannot lean on one path . it is a regularization idea . use dropout when a neural network memorizes the training set . a data scientist reaches for dropout as part of a regularization workflow .
a baseline you can explain beats a black box you cannot check .
a transformer block mixes tokens with attention and mixes features with a feed forward layer .
hierarchical clustering is a method that builds a tree of nested clusters by repeatedly merging the closest groups . it is an unsupervised learning idea . use hierarchical clustering when you want to inspect segments at several levels of detail . a data scientist reaches for hierarchical clustering as part of a unsupervised learning workflow .
attention is a mechanism that lets every token read a weighted mixture of the other tokens . it is a transformer component idea . use attention when the model has to relate tokens that sit far apart . a data scientist reaches for attention as part of a transformer component workflow .
a data science project moves through business understanding , data understanding , data preparation , modeling , evaluation and deployment .
a model is only as honest as the split it was scored on .
logistic regression is a linear model that turns a weighted sum of features into a class probability . it is a supervised learning idea . use logistic regression when you want calibrated probabilities from a simple classifier . a data scientist reaches for logistic regression as part of a supervised learning workflow .
every metric answers a different question , so pick the one that matches the cost .
data leakage is information from the target or from the future sneaking into the features . it is a model failure mode idea . use data leakage when an offline score looks too good to be true . a data scientist reaches for data leakage as part of a model failure mode workflow .
cross entropy loss is the negative log probability the model assigned to the correct token . it is an objective idea . use cross entropy loss when you are training any classifier or language model . a data scientist reaches for cross entropy loss as part of a objective workflow .
a data science project moves through business understanding , data understanding , data preparation , modeling , evaluation and deployment .
a baseline you can explain beats a black box you cannot check .
weight decay is adding a penalty on the size of the weights to the loss . it is a regularization idea . use weight decay when the model is large relative to the data . a data scientist reaches for weight decay as part of a regularization workflow .
a data science project moves through business understanding , data understanding , data preparation , modeling , evaluation and deployment .
decision tree is a model that splits the data with a sequence of threshold rules on single features . it is a supervised learning idea . use decision tree when you need a model a business reader can follow rule by rule . a data scientist reaches for decision tree as part of a supervised learning workflow .
grouped query attention is letting several query heads share one key and value head . it is a transformer component idea . use grouped query attention when you need to shrink the memory the attention cache uses . a data scientist reaches for grouped query attention as part of a transformer component workflow .
a baseline you can explain beats a black box you cannot check .
naive bayes is a probabilistic classifier that applies bayes rule and assumes the features are independent . it is a supervised learning idea . use naive bayes when you need a very fast text classifier . a data scientist reaches for naive bayes as part of a supervised learning workflow .
feature engineering is building new columns that expose the structure a model cannot find on its own . it is a preprocessing idea . use feature engineering when the raw columns hide the relationship you care about . a data scientist reaches for feature engineering as part of a preprocessing workflow .
the same preprocessing must be applied to training data and to live traffic .
learning rate is the step size that decides how far the parameters move on each update . it is an optimization idea . use learning rate when training diverges or crawls and you need to fix the step size . a data scientist reaches for learning rate as part of a optimization workflow .
one hot encoding is turning a categorical column into one binary column per category . it is a preprocessing idea . use one hot encoding when a model needs numbers but the column holds unordered labels . a data scientist reaches for one hot encoding as part of a preprocessing workflow .
the same preprocessing must be applied to training data and to live traffic .
pretraining teaches the model language , fine tuning teaches it the task .
a transformer block mixes tokens with attention and mixes features with a feed forward layer .
precision is the fraction of predicted positives that really are positive . it is an evaluation metric idea . use precision when a false alarm is expensive . a data scientist reaches for precision as part of a evaluation metric workflow .
apriori is an algorithm that mines frequent item sets and turns them into association rules . it is an unsupervised learning idea . use apriori when you want to know which products are bought together . a data scientist reaches for apriori as part of a unsupervised learning workflow .
standard scaling is rescaling a column to zero mean and unit variance . it is a preprocessing idea . use standard scaling when the model is sensitive to the scale of the features . a data scientist reaches for standard scaling as part of a preprocessing workflow .
a language model predicts the next token given every token before it .
confusion matrix is a table of true positives , false positives , true negatives and false negatives . it is an evaluation metric idea . use confusion matrix when you want to see which kind of mistake the model makes . a data scientist reaches for confusion matrix as part of a evaluation metric workflow .
training loss measures fit , validation loss measures generalization .
every metric answers a different question , so pick the one that matches the cost .
rotary position embedding is rotating the query and key vectors by an angle that depends on position . it is a transformer component idea . use rotary position embedding when you want relative position information without a learned position table . a data scientist reaches for rotary position embedding as part of a transformer component workflow .
the same preprocessing must be applied to training data and to live traffic .
recall is the fraction of the real positives that the model actually finds . it is an evaluation metric idea . use recall when a missed positive is expensive . a data scientist reaches for recall as part of a evaluation metric workflow .
lower cross entropy on held out text means the model is less surprised by it .
a model is only as honest as the split it was scored on .
gradient descent is an algorithm that walks the parameters downhill along the gradient of the loss . it is an optimization idea . use gradient descent when you are fitting any model that has a differentiable loss . a data scientist reaches for gradient descent as part of a optimization workflow .
holdout set is a slice of data that is touched only once at the very end . it is an evaluation practice idea . use holdout set when you have tuned many times and need an untouched final score . a data scientist reaches for holdout set as part of a evaluation practice workflow .
supervised fine tuning is training a pretrained model on prompt and response pairs with the loss on the response only . it is a training stage idea . use supervised fine tuning when a base model predicts text well but does not follow instructions . a data scientist reaches for supervised fine tuning as part of a training stage workflow .
pretraining teaches the model language , fine tuning teaches it the task .
a transformer block mixes tokens with attention and mixes features with a feed forward layer .
a model is only as honest as the split it was scored on .
early stopping is halting training at the step where validation loss stops improving . it is a regularization idea . use early stopping when training loss keeps falling but validation loss has turned around . a data scientist reaches for early stopping as part of a regularization workflow .
lower cross entropy on held out text means the model is less surprised by it .
a language model predicts the next token given every token before it .
every metric answers a different question , so pick the one that matches the cost .
accuracy is the fraction of predictions that are correct . it is an evaluation metric idea . use accuracy when the classes are balanced and every error costs the same . a data scientist reaches for accuracy as part of a evaluation metric workflow .
rms norm is normalizing a vector by its root mean square and rescaling it . it is a transformer component idea . use rms norm when you want layer normalization with fewer operations . a data scientist reaches for rms norm as part of a transformer component workflow .
roc auc is the chance that the model ranks a random positive above a random negative . it is an evaluation metric idea . use roc auc when you care about ranking quality rather than one threshold . a data scientist reaches for roc auc as part of a evaluation metric workflow .
overfitting is the model has learned the noise in the training set instead of the signal . it is a model failure mode idea . use overfitting when training loss is far below validation loss . a data scientist reaches for overfitting as part of a model failure mode workflow .
f1 score is the harmonic mean of precision and recall . it is an evaluation metric idea . use f1 score when you need one number that balances false alarms and misses . a data scientist reaches for f1 score as part of a evaluation metric workflow .
a language model predicts the next token given every token before it .
k nearest neighbors is a model that predicts from the labels of the closest training points . it is a supervised learning idea . use k nearest neighbors when the decision boundary is irregular and the data set is small . a data scientist reaches for k nearest neighbors as part of a supervised learning workflow .
a language model predicts the next token given every token before it .
support vector machine is a classifier that looks for the widest margin between the classes . it is a supervised learning idea . use support vector machine when you have a small clean data set with many features . a data scientist reaches for support vector machine as part of a supervised learning workflow .
backpropagation is the chain rule applied backwards through a network to get every gradient in one pass . it is an optimization idea . use backpropagation when you are training any neural network . a data scientist reaches for backpropagation as part of a optimization workflow .
a transformer block mixes tokens with attention and mixes features with a feed forward layer .
the same preprocessing must be applied to training data and to live traffic .
pretraining teaches the model language , fine tuning teaches it the task .
every metric answers a different question , so pick the one that matches the cost .
principal component analysis is a method that projects the data onto the directions that carry the most variance . it is an unsupervised learning idea . use principal component analysis when you need fewer correlated features before modeling . a data scientist reaches for principal component analysis as part of a unsupervised learning workflow .
k means is an algorithm that partitions points into k clusters around their centroids . it is an unsupervised learning idea . use k means when you want compact round segments and you can pick k yourself . a data scientist reaches for k means as part of a unsupervised learning workflow .
pretraining teaches the model language , fine tuning teaches it the task .
a data science project moves through business understanding , data understanding , data preparation , modeling , evaluation and deployment .
isolation forest is an anomaly detector that scores a point by how few random splits it takes to isolate it . it is an unsupervised learning idea . use isolation forest when you need unlabeled outlier detection on tabular data . a data scientist reaches for isolation forest as part of a unsupervised learning workflow .
dbscan is a clustering algorithm that grows clusters out of dense regions and leaves the rest as noise . it is an unsupervised learning idea . use dbscan when the clusters have odd shapes and the data contains noise . a data scientist reaches for dbscan as part of a unsupervised learning workflow .
random forest is an ensemble that averages many decision trees grown on bootstrap samples . it is a supervised learning idea . use random forest when you want a strong tabular baseline that needs almost no tuning . a data scientist reaches for random forest as part of a supervised learning workflow .
tokenization is cutting raw text into the discrete units a language model actually sees . it is a preprocessing idea . use tokenization when you are preparing text for a language model . a data scientist reaches for tokenization as part of a preprocessing workflow .
adam is a gradient descent variant that keeps a running average of the gradient and its square . it is an optimization idea . use adam when you want a robust default optimizer for a neural network . a data scientist reaches for adam as part of a optimization workflow .
missing value imputation is filling gaps in a column with a statistic or a learned estimate . it is a preprocessing idea . use missing value imputation when rows have holes and dropping them would throw away signal . a data scientist reaches for missing value imputation as part of a preprocessing workflow .
class imbalance is one class being so rare that accuracy rewards ignoring it . it is a model failure mode idea . use class imbalance when the positive class is a small fraction of the rows . a data scientist reaches for class imbalance as part of a model failure mode workflow .
underfitting is the model is too simple to capture the pattern that is really there . it is a model failure mode idea . use underfitting when training loss and validation loss are both high . a data scientist reaches for underfitting as part of a model failure mode workflow .
neural network is a stack of linear layers and nonlinear activations trained by backpropagation . it is a supervised learning idea . use neural network when the pattern is complex and you have enough data to fit it . a data scientist reaches for neural network as part of a supervised learning workflow .
a baseline you can explain beats a black box you cannot check .
train test split is holding part of the data out of training so the score is measured on unseen rows . it is an evaluation practice idea . use train test split when you need an honest estimate before you ship a model . a data scientist reaches for train test split as part of a evaluation practice workflow .
training loss measures fit , validation loss measures generalization .
